# Feature Plan — Offline mode for Android APK

**Version:** plan v2.0 (supersedes v1.0; see §0 change log)
**Date:** 2026-05-13
**Owner:** Arun (Lane L)
**Target release:** v0.6.x (patch-tier; Android-only)
**Roadmap IDs:** OFFLINE-1..OFFLINE-12 (renumbered from v1's OFFLINE-1..9)
**Plan umbrella:** `/Users/arun.prakash/.claude/plans/i-want-to-update-breezy-neumann.md`
**Source critique:** `docs/plans/v0.6.x-offline-mode-apk-SELF-CRITIQUE.md` (14 items)
**Empirical evidence (2026-05-13):**
- PDF share works on v0.5.4 APK end-to-end (capture → enrich → embed): item `caef6a71b1d4f367a08ef007` validated
- WebView lifecycle proven aggressive — `adb logcat` showed `freezing 11703` events every ~30s on the Brain process even with the app in recents
- IndexedDB does not yet exist on device (`/data/user/0/com.arunprakash.brain/app_webview/Default/IndexedDB: No such file or directory`)
- Free disk on `/data`: 79 GB; constraint is per-origin WebView quota, not physical storage
**Related code:**
- `src/components/share-handler.tsx` — share-intent capture path (PDF read via `@capacitor/filesystem` after `e7695e6`)
- `src/app/api/capture/{url,pdf,note}/route.ts` — server endpoints the APK calls
- `src/lib/capture/dedup.ts` — existing 2s dedup-window helper
- `src/lib/queue/enrichment-worker.ts` — embeds inline after enrichment (`54e7a9b`)

---

## §0. Change log — v1 → v2

| § | v1 decision | v2 decision | Reason |
|---|---|---|---|
| PDF read mechanism (A-1) | Assumed broken under v0.5.3 (`fetch(uri)` 404'd) | **Confirmed working in v0.5.4** via `@capacitor/filesystem` (commit `e7695e6`); MVP keeps PDF in scope | Empirical test today validated the fix |
| Storage budget (A-2) | Round-number guesses (50 MB cap, 20 MB PDF) | **OFFLINE-PRE: measure quota first**; cap derived from measurement, not guessed; PDFs offload to filesystem instead of IndexedDB blob column | A-2 critique + filesystem plugin is now in the build |
| IndexedDB persistence (A-3) | Assumed survives | `navigator.storage.persist()` requested at outbox-init; if `false`, user-visible warning | A-3 critique — Android WebView eviction is real |
| Background retry while app closed (B-1) | "Pure WebView, retries on `online` event" implied subway-story would just work | **Honest scoping**: v2 commits to Option A; §3.1 user story rewritten to match what Option A actually delivers ("syncs next time you open Brain"). Option C / WorkManager is explicit v0.7.x roadmap item, not vague-later | logcat evidence: WebView freezes every ~30s; subway claim was unsupportable |
| Network detection (B-2) | Browser `online` event | **`@capacitor/network` plugin** primary; browser `online` fallback secondary | Capacitor plugin is more reliable inside WebView |
| Dedup keys (B-3) | Hashed full payload, 10-min window unspecified vs 2s | **Per-kind key**: URLs key on `(kind, normalized_url)`; notes on content hash; PDFs on file SHA256. Explicit 3-tier check order (in-memory 2s → outbox 10m → server) | Critique B-3 |
| Retry budget (B-4) | MAX_ATTEMPTS=8 → ~22 min before stuck | **Network-class failures retry indefinitely** (never `stuck`); only 4xx-non-rate-limited go straight to `stuck` | Critique B-4: realistic outage windows are hours, not minutes |
| API version mismatch (B-5) | Not addressed | `X-Brain-Client-Api: 1` header; server-side 422 with `version_mismatch` code → distinct stuck state | Critique B-5 |
| Captive portal (B-6) | Claimed `content-type: text/html` detection | **Dropped claim**; classify any non-2xx that isn't auth/payload as "transient network → retry" | Critique B-6: detection is unreliable |
| Notification permission UX (C-1) | "POST_NOTIFICATIONS, gracefully degrades" | **Specified** request timing (on first successful enqueue); declined-state in-app badge becomes primary surface | Critique C-1 |
| Resume-after-kill test (C-2) | Buried in row 8 of test matrix | **Promoted** to §9.1 critical scenario + new test for partial-PDF-write resume | Critique C-2 |
| `Sync now` rate-limit interaction (C-3) | Not specified | "Sync now" respects per-item `next_retry_at` for items in 429-cooldown | Critique C-3 |
| `/inbox` page architecture (C-4) | Server component shell + client read | **Pure client component** (`"use client"`) — outbox is client-local data, no server dance | Critique C-4 |
| Prior-art evaluation (C-5) | None | **OFFLINE-1 includes 30-min prior-art spike** (Workbox bg-sync, `@capacitor-community/background-runner`, redux-persist offline) → write or adopt decision | Critique C-5 |
| Scope | OFFLINE-1..9 | OFFLINE-1..12 (added OFFLINE-PRE quota probe + OFFLINE-1 prior-art spike + OFFLINE-12 v0.7.x WorkManager roadmap entry) | Filling real gaps surfaced by critique |

---

## §1. Problem statement

The Android APK is a thin Capacitor WebView pointing at `https://brain.arunp.in`. When the user shares a URL, PDF, or note from another Android app → Brain, the capture flow does a synchronous POST to the tunnel and either succeeds immediately or fails.

Failure modes the user hits in real life:

1. **Phone has no internet** (airplane mode, subway, weak signal, data cap).
2. **Phone has internet but laptop is asleep** — Cloudflared can't reach the origin → tunnel returns 502.
3. **Phone has internet but laptop's `cloudflared` service stopped** — DNS resolves, connection refused at edge.
4. **Phone has internet, laptop awake, but `npm run dev` is not running** — Cloudflared probe returns 502 from origin.
5. **Server is up but returns a transient 5xx** (disk full, Ollama dead, enrichment queue wedged).

Today all five produce "capture failed," content is lost, user must remember to come back. That's a daily-use blocker.

## §2. Goal + non-goals

### In-scope (MVP)

- Android APK captures always succeed from the user's point of view (no "save failed" modal in the middle of a share).
- Offline / server-unreachable captures land in a local queue.
- Background retry attempts re-submit queued items opportunistically (on network regained, on app foreground, on user action, on interval — **all while the app is open**; see §4.1 honest scoping).
- User-visible inbox + nav-bar badge showing pending items + their state.
- Manual "Sync now" button to force retries.
- Clear error surface for items that have hit a terminal failure (auth bad, payload bad, version mismatch).

### Explicit non-goals

- **Background drain while the app is closed.** WorkManager / native scheduler tracked as v0.7.x roadmap (§4.10). This is a deliberate MVP scope cut after the WebView-freezing evidence; v1 plan implied it would work, v2 honestly does not.
- Full offline reading of already-captured content (separate feature; out of scope).
- Offline capture for Chrome extension or web (offline notion doesn't apply).
- Local AI enrichment on device (no on-device model).
- End-to-end encryption of the outbox (Android app-private storage already sandboxed).
- Conflict resolution beyond "first-writer-wins dedup" (no three-way merge for same-URL-twice-offline).
- iOS (no iOS target).
- Multiple offline devices syncing together (one phone).

## §3. User stories

### 3.1 "I'm on the subway and want to save a Substack post" — REWRITTEN for honesty

User opens Substack in subway, taps share → Brain. Phone has no internet. **Brain opens via share-target → enqueues to outbox → shows "Saved offline — will sync next time you open Brain" toast → control returns to Substack.**

Later that day, user opens Brain to browse their library (or shares another item). **On open, the retry trigger fires:** all queued items drain in the background while the user reads. A small badge "Synced 3 items" appears on the library.

**What v2 does NOT promise** (and v1 incorrectly implied): "automatic sync within 30s of signal returning while you're still walking around with Brain in your pocket." Logcat evidence (2026-05-13) shows the Brain WebView gets frozen by Android every ~30s when not foregrounded. JS retry timers don't fire reliably during freeze. **The MVP guarantee is "syncs reliably the next time you actively open the app."** WorkManager-backed background drain is an explicit v0.7.x feature (§4.10).

### 3.2 "My laptop is closed while I'm out"

User shares 3 articles over the morning from coffee shop. Laptop is asleep. Each share gets the "Saved offline" toast. Items queue locally. When user opens Brain later (lunch, at home) the retry triggers fire and items drain.

### 3.3 "Retries fail — I'm stuck"

Wi-Fi captive portal returns HTML on POST. Retries cycle through transient-failure backoff. **For network-class failures, retries continue indefinitely** (no MAX_ATTEMPTS — see §5.3). The user sees a persistent "1 pending sync" badge while the conditions persist; once on real internet and Brain is opened, the sync drains.

For terminal failures (rotated bearer token → 401, malformed payload → 422, API version mismatch → 422 with `version_mismatch`), the item moves to `stuck` immediately and shows a clear, actionable error.

### 3.4 "I want to manually drain the queue right now"

User knows their laptop just woke up. They open Brain, see "Pending: 3" in the nav, tap → `/inbox` page → "Sync now" button → all three drain.

## §4. Architecture

### §4.1 Honest scoping — what Option A delivers and what it doesn't

The MVP is **Option A: pure WebView + IndexedDB outbox + JS retry orchestrator**. Decision rationale (refined from v1):

| What we get | What we don't |
|---|---|
| Zero new native code; matches "thin WebView" philosophy | Background drain when app is closed/frozen — Android freezes the WebView (logcat evidence) |
| Reliable enqueue at share time (share-target launches MainActivity → WebView active) | Timed retries while app is backgrounded for >5 min |
| Reliable retry on app foreground / explicit user action | "Subway story" automatic-sync-on-signal-return |
| Survives app force-kill (queue persists in IndexedDB) | iOS support (out of scope) |

**v0.7.x roadmap (§4.10)** adds Option C: native WorkManager + Capacitor plugin bridge that reads the same IndexedDB outbox and POSTs from native code while app is closed. Architecture chosen so the v2 outbox schema and orchestrator are reused unchanged when WorkManager lands — no schema migration on the Option A → C transition.

### §4.2 Data flow

```
User shares item → CapacitorShareTarget receives intent → MainActivity launches → WebView opens
  ↓
share-handler.tsx receives event with file URI / URL / text
  ↓
Build OutboxEntry (id, kind, payload, content_hash)
  ↓
Run dedup check (§5.2) — if dup, skip
  ↓
Write entry to IndexedDB outbox (status='pending')
  ↓
[immediate] try direct POST via sync-worker
  ├─ success (2xx)  → mark synced, store server_id, show "Saved" toast
  ├─ network/5xx    → mark queued, show "Saved offline" toast
  └─ 401/422/4xx    → mark stuck, show error toast
  ↓
return control to user (immediate UI feedback regardless of network)

Retry triggers (only when WebView is active and not frozen):
  - @capacitor/network → networkStatusChange to 'wifi' or 'cellular'
  - @capacitor/app    → appStateChange to active
  - 30s in-app interval (only ticks while WebView unfrozen)
  - User: tap "Sync now"
  ↓
For each queued entry where next_retry_at <= now:
  - try POST
  - on success → mark synced
  - on network/5xx → bump attempts, set next_retry_at = now + backoff(attempts)
  - on terminal 4xx → mark stuck
```

### §4.3 Outbox schema (IndexedDB)

```typescript
// IDB store: 'outbox', keyPath: 'id'
interface OutboxEntry {
  id: string;                 // client UUID
  kind: 'url' | 'pdf' | 'note';
  payload: object;            // the request-body verbatim (JSON-safe)
  file_path?: string;         // for PDFs: filesystem path under app-private dir (NOT base64 in IDB)
  file_name?: string;         // for PDFs
  content_hash: string;       // dedup key (see §5.2)
  status: 'pending' | 'queued' | 'synced' | 'stuck' | 'duplicate';
  status_reason?: 'auth_bad' | 'payload_bad' | 'version_mismatch' | 'transient'; // when status='stuck' or 'queued'
  attempts: number;           // retry counter
  created_at: number;         // ms epoch
  last_error?: string;        // user-readable; reset to undefined on success
  next_retry_at?: number;     // ms epoch; ignored unless status='queued'
  server_id?: string;         // populated on success → ties to items.id
}
// Indexes: by status, by created_at, by content_hash
```

**Why PDFs go to filesystem, not blob in IDB:** A-2 / A-3 critique. PDFs can be 5–10 MB; storing in IDB blobs hits the per-origin WebView quota fast and is more vulnerable to eviction than `Filesystem.writeFile` to app-private storage. The outbox row holds the path; the actual bytes live in app-private files.

### §4.4 Retention

- `status='synced'`: delete after 24 hours (user can review what synced recently in `/inbox`).
- `status='stuck'`: retain forever until user acts (Retry / Discard).
- `status='duplicate'`: delete after 1 hour (audit only; not user-facing).
- Filesystem PDFs cleaned up alongside their outbox row.

## §5. Design decisions

### §5.1 Storage strategy: IndexedDB metadata + Filesystem blobs

Switch from "everything in IDB" (v1) to "metadata in IDB, blobs in filesystem":

| Data | Where | Why |
|---|---|---|
| Outbox row (id, kind, payload, status, etc.) | IndexedDB store `outbox` | structured query, sort by status |
| URL/note payloads | Inline in IDB row | small (<10 KB) |
| PDF bytes | `@capacitor/filesystem` writeFile to `Directory.Data` | survives quota pressure better; doesn't bloat IDB |
| Persistence request | `navigator.storage.persist()` at outbox-init | A-3: ask Chromium not to evict the outbox under memory pressure |

If `persist()` returns `false` (some Android WebView versions silently refuse), surface a one-time toast: "Your device may not preserve offline saves across long backgrounds. Sync as soon as you can."

### §5.2 Dedup (refined per critique B-3)

**Three-tier check order, applied in this exact sequence:**

1. **In-memory 2s window** (existing `isDuplicateShare()` at `src/lib/capture/dedup.ts:39`). Catches accidental double-taps. **Key:** `shareDedupKey(kind, hashPayload(payload))`. Unchanged from v0.5.0.
2. **Outbox 10-minute window.** Computed dedup key per kind:
   - `url` → `sha256(normalize_url(payload.url))` — strips utm_*, fragments, trailing slashes
   - `note` → `sha256(payload.title + payload.body)`
   - `pdf` → `sha256(file_bytes)` — content hash via Filesystem readFile + crypto
3. **Server dedup.** Existing `findItemByUrl()` on `/api/capture/url:83` already does this; on `200 { duplicate: true, itemId }` the outbox row marks `synced` with the server id (no client-side error).

**On dedup hit (steps 1 or 2):** silently no-op. No outbox entry created. No user-facing toast.

### §5.3 Retry classification (refined per critique B-4 + B-6)

| HTTP / network condition | Class | Action |
|---|---|---|
| `fetch()` throws (DNS, socket, timeout, captive portal HTML) | transient | retry indefinitely with backoff cap (no `stuck`) |
| 5xx (incl. 502, 503, 504) | transient | retry indefinitely |
| 429 with `Retry-After` | transient | retry after `Retry-After` seconds (or 60s default) |
| 401 / 403 (bearer auth) | terminal | `stuck`, reason `auth_bad`, "Re-pair your device in Settings" |
| 422 with `code: version_mismatch` | terminal | `stuck`, reason `version_mismatch`, "Update Brain to sync these items" |
| 422 / 400 (any other reason) | terminal | `stuck`, reason `payload_bad`, "Item couldn't be saved" |
| 200/201 with `duplicate: true` | success | `synced` with returned `itemId` |
| 200/201 fresh | success | `synced` |

**Backoff for transient failures:** exponential with jitter, capped at 1 hour. Base 10s.

```
attempt 1  → +10s
attempt 2  → +20s
attempt 3  → +40s
attempt 4  → +80s
attempt 5  → +160s (~3 min)
attempt 6  → +320s (~5 min)
attempt 7  → +640s (~10 min)
attempt 8  → +1280s (~21 min)
attempt 9+ → +3600s (1 hour cap)
```

**No MAX_ATTEMPTS for transient failures.** A subway-trip-turned-flight outage of 4 hours is realistic; v1's 22-minute cap was wrong. The badge stays visible until success or user explicitly discards.

When `@capacitor/network` reports network-state-change to online, every queued entry's `next_retry_at` resets to "now" — opportunistic retry on connectivity regain.

### §5.4 Network detection (B-2)

Primary: **`@capacitor/network`** plugin. Fires `networkStatusChange` events through the JS bridge — more reliable than browser DOM `online`/`offline` events inside Capacitor WebViews.

Fallback: browser `window.online` event (defensive — if Capacitor plugin fails to register).

Status query: `Network.getStatus()` returns `{ connected, connectionType }` synchronously for "should I bother trying" pre-flight checks.

### §5.5 API version compatibility (B-5)

Every outbox POST sends `X-Brain-Client-Api: 1` header. Server enforces:
- `/api/capture/{url,pdf,note}` reads header; if present and != current expected version, returns `422 { code: 'version_mismatch', message: 'Update Brain APK to sync these items' }`.
- Initial expected version: `1`. Bumped only when a breaking change requires re-encoding old queue entries.

This protects against the scenario where a user queues 30 items offline, doesn't open the app for a week, server gets upgraded with a breaking schema change in the meantime.

### §5.6 Notification UX (C-1)

- **Permission request:** fire on the *first successful enqueue* (not on app first-launch — too soon, user hasn't opted into the feature). Use `@capacitor/local-notifications.requestPermissions()`.
- **Granted path:** Android system notification fires on `0 → ≥1 stuck` transition. Tap → `/inbox`.
- **Declined path:** in-app nav-bar badge is the primary surface. Badge stays visible until count returns to 0.

### §5.7 `/inbox` page (C-4)

`src/app/inbox/inbox-client.tsx` — **pure client component**, marked `"use client"`. No server-side data fetch. Reads outbox via IDB, renders grouped by status, exposes per-item Retry / Discard + global Retry-all.

`src/app/inbox/page.tsx` is a thin server component that just renders `<InboxClient />` (no async data fetch on the server).

### §5.8 Sync-now rate-limit handling (C-3)

"Sync now" iterates all `queued` entries. For each:
- If `next_retry_at <= now`, try the POST.
- If `next_retry_at > now` (still in 429 cooldown), skip and surface "1 item is rate-limited; try again in Xs" inline.

This prevents users from hammering the server during a 429 backoff and extending the rate-limit window.

## §6. Platform scope

- **In:** Android APK (Capacitor WebView), specifically `window.Capacitor?.isNativePlatform() === true`.
- **Out:** desktop web, Chrome extension, iOS.

Gating: all outbox-related code lives behind the existing `isNativePlatform()` check. In non-APK contexts the feature is inert; share-handler does direct POSTs as today.

## §7. Rollout — 12 commits

Each commit gates on §9 unit tests + the relevant §9.3 manual scenario. Lane L patch bumps allowed under tiered rule `48967cd`.

| # | Commit | Roadmap ID | Scope |
|---|---|---|---|
| 1 | `feat(offline): pre-execution prior-art spike (OFFLINE-PRE)` | OFFLINE-PRE | Write `docs/research/offline-queue-prior-art.md` evaluating Workbox bg-sync, `@capacitor-community/background-runner`, redux-persist-offline. Verdict: adopt, partial-adopt, or roll-own with rationale. |
| 2 | `feat(offline): IndexedDB outbox schema + storage layer (OFFLINE-1)` | OFFLINE-1 | `src/lib/outbox/{storage.ts,types.ts,storage.test.ts}` — `idb` wrapper, fake-indexeddb tests; `navigator.storage.persist()` |
| 3 | `feat(offline): dedup keys + backoff math + classifier (OFFLINE-2)` | OFFLINE-2 | `src/lib/outbox/{dedup.ts,backoff.ts,classify.ts}` + tests |
| 4 | `feat(offline): sync worker orchestrator (OFFLINE-3)` | OFFLINE-3 | `src/lib/outbox/sync-worker.ts` + tests; covers every row of §5.3 table |
| 5 | `feat(offline): wire share-handler into outbox (OFFLINE-4)` | OFFLINE-4 | `src/components/share-handler.tsx` enqueue path; manual test scenario |
| 6 | `feat(offline): Capacitor.network triggers + foreground retry (OFFLINE-5)` | OFFLINE-5 | `src/lib/outbox/triggers.ts`; install `@capacitor/network` |
| 7 | `feat(offline): API version header (OFFLINE-6)` | OFFLINE-6 | `X-Brain-Client-Api` header on outbox POSTs; server-side `/api/capture/*` accept logic; 422 with `version_mismatch` |
| 8 | `feat(offline): inbox client page + nav badge (OFFLINE-7)` | OFFLINE-7 | `src/app/inbox/{page.tsx,inbox-client.tsx}` — pure client component; sidebar badge |
| 9 | `feat(offline): Android local notifications for stuck (OFFLINE-8)` | OFFLINE-8 | `src/lib/outbox/notifications.ts`; install `@capacitor/local-notifications`; `POST_NOTIFICATIONS` permission |
| 10 | `feat(offline): PDF filesystem-blob path + cleanup (OFFLINE-9)` | OFFLINE-9 | `Filesystem.writeFile` for PDFs at enqueue; sync reads back; cleanup on synced/discard |
| 11 | `feat(offline): plain-language copy + 0.5.5 release (OFFLINE-10)` | OFFLINE-10 | All toast / inbox / notification strings polished; `package.json` 0.5.4 → 0.5.5 |
| 12 | `docs(offline): v0.7.x WorkManager roadmap entry (OFFLINE-12)` | OFFLINE-12 | `docs/plans/v0.7.x-offline-workmanager-roadmap.md` — sketches the Option C upgrade path, identifies what stays and what changes |

OFFLINE-11 (manual-verification matrix) is a verification gate run during commits 5–11; not a separate code commit.

## §8. Performance + storage budget

### §8.1 Pre-execution measurement (OFFLINE-PRE/A-2)

Before OFFLINE-1 lands, write `scripts/measure-webview-quota.html` — a tiny standalone HTML the user loads inside Brain WebView once. It calls `navigator.storage.estimate()` and reports `{ usage, quota }` to the laptop via the existing `/api/errors/client` endpoint.

Decision derived from measurement:
- if `quota >= 500 MB` → use generous limits (200 MB outbox cap)
- if `quota` 100–500 MB → moderate (50 MB cap)
- if `quota < 100 MB` → conservative; may need to skip MVP PDF support and defer to v0.7.x WorkManager (which can write directly to filesystem without WebView quota)

### §8.2 Runtime budgets

| Path | Budget | Measurement |
|---|---|---|
| Outbox cold open (first share) | < 100 ms | `performance.now()` |
| Enqueue single URL/note entry | < 20 ms | same |
| Enqueue PDF entry (filesystem write + IDB row) | < 500 ms for 5 MB PDF | same |
| Sync-worker cycle (10 queued, all 2xx) | < 3 s | wall clock; network-bound |
| Sync-worker CPU cost (excl. network) | < 200 ms | same |
| `/inbox` cold render with 50 entries | < 800 ms | devtools perf |
| Storage cap (per OFFLINE-PRE measurement) | derived | warn at 80%; reject new enqueue at 100% |
| Per-PDF max | 25 MB | reject larger with clear message |

Failing any budget in manual testing → feature does NOT ship; design revision.

## §9. Testing + gates

### §9.1 Critical scenarios (must pass before each commit ships)

**Promoted from v1 row 8** per critique C-2:

1. **Resume-after-kill.** Enqueue 3 items offline → swipe Brain out of recents → reopen → all 3 still queued and drain on connectivity. **Plus partial-write variant**: force-kill mid-enqueue (during PDF filesystem write); on reopen, partial entry is either resumed correctly or safely discarded (not leaked).
2. **Wrong bearer token.** Rotate token on laptop → share → outbox row goes `stuck` with `auth_bad`; toast shows "Re-pair in Settings."
3. **Captive portal.** Connect to coffee-shop Wi-Fi with login redirect; shares queue and retry indefinitely; once on real internet, drain.

### §9.2 Unit tests

- `src/lib/outbox/storage.test.ts` — IDB ops via `fake-indexeddb`
- `src/lib/outbox/dedup.test.ts` — per-kind keys, time-window boundaries, normalize-url canonicalization
- `src/lib/outbox/backoff.test.ts` — exponential math, jitter-deterministic-by-seed for tests, 1-hour cap
- `src/lib/outbox/classify.test.ts` — every row of §5.3 table including new `version_mismatch` branch
- `src/lib/outbox/sync-worker.test.ts` — orchestrator with mock fetch + mock storage; covers transient/terminal split

### §9.3 Regression gates (must be green per commit)

- `npm test` — all tests
- `npm run typecheck` + `npm run lint`
- `npm run smoke` — all 4 smoke suites
- `npm run build:apk` — APK builds + unlocks cleanly on Pixel
- Existing 260+ tests stay green

### §9.4 Manual verification matrix (full sweep before OFFLINE-10 patch release)

| Scenario | Expected |
|---|---|
| Share URL, phone offline | "Saved offline" toast; outbox `queued` |
| Share URL, phone online, server up | "Saved" toast; outbox `synced` |
| Share PDF, phone offline | filesystem write + IDB row; "Saved offline" |
| Share PDF > 25 MB | rejected with clear size message |
| Share same URL twice within 2s | second is silent no-op (in-mem dedup) |
| Share same URL twice within 10 min | second is silent no-op (outbox dedup) |
| Open Brain with queued items, online | drain within 3s; "Synced N items" badge clears |
| Force-kill app with items queued, reopen | queued items still present; resume |
| Rotate bearer token → share | `stuck` with auth_bad; clear message |
| Server returns 422 (any non-version reason) | `stuck` with payload_bad |
| Server returns 422 with `version_mismatch` | `stuck` with version_mismatch; "Update Brain" copy |
| Captive portal | retries indefinitely; eventual success when on real internet |
| Notification permission declined | feature still works; in-app badge primary surface |
| `/inbox` page | shows entries grouped by status; per-item Retry / Discard work; Retry-all works |
| Storage approaching cap (80%) | warning toast; no further enqueue at 100% |

## §10. Risks + mitigations

| Risk | Severity | Mitigation |
|---|---|---|
| WebView quota too small per OFFLINE-PRE measurement | High | OFFLINE-PRE pre-flight; if < 100 MB, scope-cut to URL+note only and defer PDF to v0.7.x |
| `navigator.storage.persist()` returns false on Android WebView | Medium | one-time toast warning user; documented limitation |
| WorkManager roadmap items pile up before v0.7.x | Low | OFFLINE-12 explicitly captures the gap; user can choose to bring forward if pain shows |
| `@capacitor/network` plugin doesn't fire reliably | Low | browser `online` event fallback retained |
| Server `version_mismatch` deployment forgets to bump expected version | Low | Server defaults to "accept any client-api header" until explicitly tightened in a future server change |
| User confused by "queued" vs "stuck" state language | Medium | OFFLINE-11 copy polish; user-test all four states with you |
| Notifications spam for repeated stuck transitions | Low | Only fire on 0→≥1 transition; not on each retry; debounce 30s |
| IDB blob race between sync attempt and PDF cleanup | Low | sync-worker reads file before marking synced; cleanup is post-success |
| Dedup miss because URL has random tracking params | Low | normalize_url() strips utm_*, fbclid, gclid, ref, source, fragments |

## §11. Open questions for user (must resolve before OFFLINE-PRE ships)

1. **Quota-pressure scope cut.** If OFFLINE-PRE measurement reveals < 100 MB IDB quota, are you OK with v2 MVP shipping URL + note only (PDF deferred to v0.7.x WorkManager)?
2. **Notification permission timing.** Plan says "request on first successful enqueue." Is that acceptable, or do you want it deferred until first stuck-state (so users only see the prompt if it actually matters)?
3. **Discard semantics.** Tap "Discard" on a stuck item: hard delete, or move to a `discarded` status for audit-trail / undo? MVP proposes hard delete.
4. **WorkManager urgency.** Honest scoping (§3.1) means subway-style auto-sync waits for v0.7.x. Is that acceptable, or should we bring v0.7.x WorkManager forward into v0.6.x as part of this plan?
5. **Prior-art adoption (OFFLINE-PRE).** If the spike concludes Workbox bg-sync covers ~70% of the use case, are you OK adopting it (adds a npm dep but cuts ~600 LOC) vs rolling our own?

## §12. What this plan does NOT do

- No offline reading of already-captured content. Library view requires the tunnel.
- No full offline RAG / Ask. Ask always requires the server.
- No outbox encryption at rest (Android app-private storage handles this).
- No opt-out toggle (always on; invisible when online).
- No cross-device sync.
- No iOS build.
- **No background drain while app closed/frozen** — explicit v0.7.x deferral with WorkManager (§4.10 / OFFLINE-12).

## §13. Definition of done

### §13.1 Per-commit DoD

- §9.2 + §9.3 gates green
- The §9.1 critical scenarios that this commit unblocks must pass on real device
- Commit message in Lane L style (why + what + trade-offs)

### §13.2 Feature-closeout DoD

- All 12 OFFLINE IDs implemented
- OFFLINE-PRE measurement document committed at `docs/research/webview-quota-pixel-2026-05-13.md` or similar
- OFFLINE-1 (prior-art spike) document committed at `docs/research/offline-queue-prior-art.md`
- User has manually verified ALL §9.4 matrix rows on real Pixel
- `RUNNING_LOG.md` entry describes ship + learnings + WorkManager v0.7.x trail
- Plan doc status updated to `**Status:** SHIPPED — <commit SHA>` (separate follow-up commit)
- v0.5.5 patch tag; root `package.json` 0.5.4 → 0.5.5

## §14. Feature ID map

| ID | Description | Commit # |
|---|---|---|
| OFFLINE-PRE | WebView quota measurement script + decision doc | 1 |
| OFFLINE-1 | Prior-art spike + storage layer (IndexedDB + persist + types) | 1, 2 |
| OFFLINE-2 | Dedup keys + backoff math + classifier | 3 |
| OFFLINE-3 | Sync-worker orchestrator | 4 |
| OFFLINE-4 | Share-handler outbox wiring | 5 |
| OFFLINE-5 | Capacitor.network + foreground retry triggers | 6 |
| OFFLINE-6 | API version header + server validation | 7 |
| OFFLINE-7 | `/inbox` client page + nav-bar badge | 8 |
| OFFLINE-8 | Android local notifications for stuck transitions | 9 |
| OFFLINE-9 | PDF filesystem-blob path | 10 |
| OFFLINE-10 | Plain-language copy + v0.5.5 release | 11 |
| OFFLINE-11 | Manual-verification matrix (verification gate) | every commit 5–11 |
| OFFLINE-12 | v0.7.x WorkManager roadmap doc | 12 |
