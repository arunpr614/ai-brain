# v0.6.1.1 — Hotfix plan

**Status:** PROPOSED — one ASK below
**Sources:** RUNNING_LOG #50 (Anthropic 529), #52 (retrieve revalidation)

## T-1 — Anthropic 529 retry

- **File:** `src/lib/llm/anthropic.ts` — `generate()` (`:155`), `generateStream()` (`:192`). `generateJson()` inherits via `generate()`.
- **What:** Retry on `res.status` ∈ {429, 503, 529} and on `LLMError("connection")`. Pass-through on all other 4xx/5xx. Caller `AbortSignal` aborts immediately, mid-backoff included.
- **Out of scope:** Batch endpoints (`submitBatch`/`pollBatch`) — different code path, today's bug is the single-request Ask path.
- **Test:** `src/lib/llm/anthropic.test.ts` — retry-then-success, retry-exhausted-throws, 4xx-pass-through, abort-mid-backoff.
- **Acceptance:** Adapter resolves a stubbed 529→200 sequence in unit test. Manual: trigger an Ask during the next observed 529 window and confirm UI no longer hangs.

## T-6 — Push item filter into retrieve JOIN

- **File:** `src/lib/retrieve/index.ts:96-122`.
- **What:** When `opts.itemId` is set, scope the inner vec0 scan with `c.item_id = ?` so we rank chunks within the item, not globally. Drop the JS-side `scoped` filter for that branch. Un-scoped path unchanged.
- **Index check:** `idx_chunks_item_id ON chunks(item_id)` exists (`src/db/migrations/001_initial_schema.sql:48`). No new index needed.
- **Test:** Extend `src/lib/retrieve/index.test.ts` — fixture with one multi-chunk item + one 1-chunk item, generic query scoped to the 1-chunk item, assert ≥ 1 result.
- **Acceptance:** Re-run entry-#52 repro on staging — 1-chunk item + generic question returns ≥ 1 chunk.

## T-R — Release

Bump `package.json` to `0.6.1.1`. `npm run typecheck && npm run lint && npm test`. Deploy. Tag `v0.6.1.1` after `/api/health` 200.

## ASK

**Retry budget for T-1:** how many attempts and what max added latency on the fail path? (My instinct is 3 attempts / ~3 s cap with `Retry-After` honored when present, but pick the budget — I'll fit the schedule to it.)
