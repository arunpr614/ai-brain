# Work Completed and Delivered

## Reading rule

This document separates:

- **pushed evidence**: committed on the branch and visible in draft PR #57;
- **accepted evidence**: independently reviewed for a stated bounded authority;
- **live uncommitted work**: present in the worktree but not yet integrated or accepted; and
- **not done**: work that must not be inferred from foundations.

## Stage 0: baseline and reconciliation

### Completed

- Located the AI Brain checkout and created the requested isolated Phase4 worktree/branch.
- Preserved unrelated user work.
- Inventoried 140 reference paths, 126 unique hashes, and 14 duplicate groups with zero missing sources.
- Reconciled the planning/research stack and merged PR lineage.
- Recorded source precedence and material conflicts.
- Audited the current migration runner, migration inventory, historical duplicate prefixes, and collision rules.
- Initially allocated the then-safe 027/028/029 sequence and recorded the shift-together rule.
- Reconciled later protected-main drift: ordinary `027_notebooklm_url_sources.sql` now exists, so D-020 shifts the feature sequence to 028/029/030.
- Froze the implementation baseline, affected subsystem inventory, release authority, risk register, decision log, tracker, and P0 traceability.
- Completed the Stage 0 initial adversarial review and focused recheck.

### Accepted authority

Stage 0 produced a conditional GO for the narrowly defined Stage 1 containment slice. It did not authorize feature schema, extension behavior, lab access, or production feature enablement.

## Stage 1: backward-compatible containment

### Completed implementation themes

- Authoritative deployment classification with production-denial precedence.
- Old-schema absent/ready/incompatible capability handling.
- Pre-body restricted-capability denial.
- Worker-mode resolution before content-worker imports.
- Existing claimant, batch, upgrade, repair, and backfill containment.
- Kill-switch and authority ordering.
- Configured-origin and private/no-store response helpers.
- Content-free diagnostics and privacy redaction.
- Durable enrichment-batch reservation before provider dispatch.
- Mixed-artifact release capability attestation and rollback refusal.
- Marker/state drift hardening across realtime, scheduled, and batch paths.

### Review outcome

The initial Stage 1 gate found one rollback P1. D-019 bound reservation-awareness to the packaged application rather than the release-tool builder, added activation/stopped-writer/restoration checks, and refused historical unaware artifacts against live unresolved markers. The focused final recheck returned GO with zero P0/P1 findings.

### Authority

Stage 1 is accepted only as non-enabling containment. It preserves ordinary behavior while denying restricted capture/manual work. It does not implement the user-facing features.

## Stage 2: contract and prerequisite foundations

### Physical-schema contract

The 16,988-line Stage 2 physical-schema addendum was repeatedly revised and independently reviewed. The accepted exact files are:

- addendum `d1eef042423d7d2d3413637f62bd8ed5842b64846db6f656a0b8e1cb7e5eed48`;
- acceptance registry `7b022d82e855891eb1a818df9c09ab070586c13beea7acf6a8c003d845be9f45`;
- static authority index `e691edfd0edcade37d439906f3b97dee9a3b05d57baae995ae23fb0254bbfd5d`;
- verifier `133d55fae063244e7c6d413c64acbe88ebf0d1e83736296d9a00ca00de2e68a6`.

All four are content-hash frozen. Local read-only modes are best-effort guards;
Git records ordinary file modes and does not preserve `0444`. One durable final
gate, informed by three independent session review passes, returned Contract GO
with no unresolved P0/P1. This is a design/contract gate only.

### Committed foundation primitives

The branch contains:

- exact fingerprint framing;
- ordinary-startup migration admission;
- a bounded no-history item-instance allocator;
- frozen-registry evidence protocol primitives;
- a deny-only synthetic scope-policy primitive;
- a private memory-only native bridge feasibility route;
- a private nominal file-backed factory route;
- product-test routing that separates portable and native nominal suites;
- a hosted `macos-26` native nominal job required by strict verification; and
- documentation classification for the new feature implementation corpus and commands.

### Memory-only native route

The memory-only route was hardened to a v5 envelope with 24 v4 scenarios. It binds:

- exact frozen zero-input native ownership;
- finalized transaction evidence;
- exact constraint/authorization mechanisms;
- six one-field bind refusals;
- distinct replay, reset, and rebind attempts;
- sealed worker/process isolation; and
- no handle, path, process ID, migration, readiness, or production authority.

One durable final gate, informed by three independent session review passes,
returned bounded GO. The route remains memory-only and cannot prove
file-backed stopped-writer behavior.

### Nominal file-backed factory

At pushed head `4786b079e88cc01ec8e9c300faa93e3832ae2678`:

- the portable suite passed 1,251 tests with zero failures/skips;
- the local Darwin native nominal suite passed 30 tests with zero failures and 3 explicit skips;
- the two P1 skips were abrupt-stop/restart cases;
- the P2 skip was broader synthetic native prepare/finalize fault injection;
- exact-head Product CI run [30245448800](https://github.com/arunpr614/ai-brain/actions/runs/30245448800) passed;
- native nominal job `89911374316` passed;
- strict required `verify` job `89911975927` passed;
- Agent documentation run [30245448803](https://github.com/arunpr614/ai-brain/actions/runs/30245448803), job `89911374473`, passed; and
- release packaging jobs were skipped as expected for this branch.

Nominal evidence deliberately reports `abruptExitRestart=false`.

## Pushed implementation commit map

| Commit | Purpose |
|---|---|
| `e97a1b5` | Freeze Stage 0 reconciliation contracts |
| `013e3bc` | Add Stage 1 authority foundations |
| `66d1cce` | Contain claimants and reserve dispatches |
| `f8717c0` | Redact private processing diagnostics |
| `93a9073` | Attest rollback compatibility |
| `bd0fb7b` | Record Stage 1 formal GO evidence |
| `ff3a425` | Integrate audited ordinary-schema-027 frontier |
| `fc00053` | Freeze Stage 2 physical contract |
| `e731182` | Add Stage 2 fingerprint framing |
| `c722b3d` | Gate Stage 2 migration startup |
| `26245cb` | Add bounded item-instance allocator |
| `3e8e56b` | Add Stage 2 evidence protocol primitives |
| `5be5985` | Prove disposable native bridge route |
| `aa4baa3` | Record Stage 2 foundation wave |
| `60de5be` | Add deny-only Stage 2 scope policy |
| `fb1517a` | Record Stage 2 scope primitive |
| `d885007` | Seal hardened native bridge evidence |
| `9567d8f` | Record native-route gate |
| `6e61eee` | Add nominal file-factory CI proof |
| `39bfad9` | Restore clean-scope CI typecheck |
| `1fb8226` | Classify implementation CI artifacts |
| `4786b07` | Record the exact-head nominal CI gate in the running log |

The branch is pushed through `4786b07`. No later worktree changes are committed.

## Live uncommitted native work observed in the session

The native crash/recovery specialist produced four bounded files before the
goal changed to handover documentation:

| File | Current SHA-256 | Current result |
|---|---|---|
| `native/brain-s28-file-factory/src/brain_s28_file_factory.cpp` | `5d9ee11ea7cf657635e8500ee3723a8126ba03a3d8f8b5422cec58929911df9f` | Four compile-time profiles; nominal fsync trace; two direct crash/recovery pairs |
| `native/brain-s28-file-factory/file-factory-source-manifest.json` | `f6eaa1a45dfbc109606d189841b4e8af1972478baaf6da4f3c7ec76fdda2001b` | Updated nominal source binding |
| `native/brain-s28-file-factory/file-factory-crash-recovery-source-manifest.json` | `9dd35f94a4a0676196d4942818a85c5b7aea06c60388814b7befd372345a0407` | Closed profile/source contract |
| `scripts/build-youtube-stage2-file-factory-crash-recovery.mjs` | `172a02763130d123094f025cff87e37a02b21ece31cc64d4f7b76643df37ee2c` | Candidate deterministic profile-bound builder; direct session checks only; integration and acceptance pending |

Session-derived direct specialist evidence, not a persisted integrated gate:

- crash after durable create exits 86 with zero stdout/stderr;
- fresh durable recovery attests exact zero-byte main-only residue, no sidecars, `walRecoveryRequired=false`, identity-safe cleanup, and an empty root;
- crash while `BEGIN IMMEDIATE` is held exits 87 with zero stdout/stderr and canonical main/WAL/SHM residue;
- fresh held-writer recovery performs `quick_check(1)`, proves a new writer can acquire and roll back, closes cleanly, removes owned leaves safely, and leaves the root empty;
- all four profiles compile deterministically;
- invalid/multiple profiles and ambient compiler overrides fail closed; and
- no harness build/root/process residue remained after the direct checks.

These observations were reported from direct specialist invocations during the
session. They were not persisted as an integrated command transcript, have not
been integrated into the TypeScript controller/test receipt, and have not been
independently re-reviewed as an exact slice.

Review-remediation evidence recorded on 2026-07-27 at 17:11 IST:

- `scripts/run-youtube-stage2-file-factory-proof-worker.mjs` now includes both
  fsync events and hashes to
  `e274ab7179a1c29118b358edf19fa2eb2bd1defc062302f5916dac3e23e2ba74`;
- `src/db/stage2/file-factory-proof.test.ts` now pins the current trace digest
  and hashes to
  `44dba3abd568cd4d409918dd167bfc2fde815bc443853596a66d919d6d78eb9e`;
- the 265-entry source trace canonically hashes to
  `1bca0c280eef643bf7b286973a70d59eed1cc08650f20791315b5b107b9cdbc7`;
  and
- `npm run test:stage2-native:nominal` passed 30, failed zero, and retained the
  three explicit gap skips.

This persisted command result repairs the nominal mismatch only. It does not
validate the candidate crash/recovery workers or satisfy the missing
crash/restart gate.

## Work deliberately not completed

- No migration 028 SQL exists.
- No migration was applied.
- No S28 package/runtime readiness was established.
- No fixed SQLite source was selected or integrated.
- No complete database-actor/checkpoint inventory exists.
- No Chrome manifest, side-panel, extractor, grant, or commit implementation exists.
- No exact-item user-facing recovery implementation exists.
- No link-only implementation exists.
- No held manual-enrichment service or worker exists.
- No final UX/accessibility implementation exists.
- No packaged extension E2E for these features exists.
- No live YouTube request or lab canary occurred.
- No transcript content was captured or processed.
- No production database, provider, configuration, or extension publication was touched.
- No Wiki update was published.
- No merge or deployment occurred.

## Invalidated or superseded evidence

- Earlier hosted failures at `6e61eee` and `39bfad9` are superseded by the green exact-head runs at `4786b07`.
- Earlier Stage 2 contract review hashes are superseded by the final accepted `d1eef042...` seal.
- The original 027/028/029 feature migration allocation is superseded by D-020's 028/029/030 shift.
- Historical code audits pinned to `cbaed78` are not current-main behavior.
- The earlier dirty-tree 25-pass/5-fail/3-skip nominal result is superseded by
  the timestamped 30-pass/0-fail/3-skip remediation rerun above. The two P1
  crash/restart gaps and the P2 synthetic-fault gap remain explicit skips.
- The D-021 addendum's first review returned NO-GO; its current prose and hash are not accepted evidence.
