{
  "name": "brain-extension",
  "version": "0.7.4",
  "private": true,
  "description": "Chrome MV3 extension for Brain capture and local, approved NotebookLM deliveries.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.4.0",
    "@types/chrome": "^0.0.270",
    "tsx": "^4.20.0",
    "typescript": "^5.4.0",
    "vite": "^6.4.3"
  }
}
