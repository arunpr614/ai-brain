# Code Review: codex/v0.8.0-safe-capture-upgrades

**codex/v0.8.0-safe-capture-upgrades → origin/main** | **22 files** | **+1781/-133**

## Code Review

💬 **COMMENT**

> ⚠️ *AI-generated verdict. This is a starting point for discussion, not a substitute for human review.*

## Summary

| Severity | Count | Summary |
|----------|-------|---------|
| 🟡 Medium | 1 | The new upgrade affordance can be shown for Substack paywall previews even though that submit path cannot upgrade them. |

**What this change does well:**

The core YouTube/Shorts and LinkedIn upgrade path is conservative and well covered: weak metadata-only captures can be upgraded with meaningful pasted text, strong captures are protected from overwrite, derived enrichment/search/vector state is reset, and the API/Telegram paths have focused regression coverage. The DB cleanup path also correctly avoids stale FTS and stale batch polling based on the tests and migration triggers.

**What should be addressed:**

Fix the UI/policy mismatch before treating the item-detail upgrade form as production-ready. Either keep Substack in a general "Needs upgrade" library filter but do not render the pasted-text form for it, or implement the promised Substack email/body upgrade path so the visible form can actually succeed.

## Findings

### Quality

#### [MEDIUM] QUAL-UX-UPGRADE: Substack paywall previews are marked as needing an upgrade even though the new pasted-text upgrade action cannot upgrade Substack items.
**File**: `src/lib/capture/upgrade-policy.ts:96`

**Suggestion**: Split the concepts of "needs upgrade" and "can upgrade with pasted text", or remove Substack from this helper until an email/paste upgrade path exists. The detail page should only render UpgradeTextForm for platforms handled by buildUpgradeContent, while the Library can still have a separate "Needs upgrade" filter if it needs to surface Substack previews.

---

### Adherence

_Skipped - no ticket provided. Run with `--ticket <issue-key>` to check adherence._
